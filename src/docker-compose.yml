services:
  postgres:
    container_name: postgres-sql
    image: postgres
    environment:
      POSTGRES_USER: oumaa
      POSTGRES_PASSWORD: oumaapass
      PGDATA: /data/postgres
    volumes:
      - postgres:/data/postgres
    ports:
      - "5432:1234"
    restart: unless-stopped

volumes:
  postgres: